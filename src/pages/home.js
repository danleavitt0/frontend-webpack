import element from 'vdux/element'
import Toolbar from '../components/toolbar'

function render ({props}) {
  return <div>
    <Toolbar
      title='Cycle Shell'/>
  </div>
}

export default {
  render
}
